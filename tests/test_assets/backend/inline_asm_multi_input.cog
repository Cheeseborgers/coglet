main::() -> s32 {
    a: s64 = 20;
    b: s64 = 22;
    result: s64 = 0;

    asm volatile (
        "movq %1, %0\n\taddq %2, %0"
        : "=r"(result)
        : "r"(a), "r"(b)
    );

    if (result != 42)
        return 1;
    return 0;
}
