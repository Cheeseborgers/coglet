Plain::struct {
    value: c_int;
}

Kind::enum(c_int) {
    A = 0,
}

#repr(c)
Bad::struct {
    unsized: i32[];
    zero: i32[0];
    nested: Plain;
    plain_array: Plain[2];
    enumeration: Kind;
    enum_array: Kind[2];
    plain_pointer: Plain*;
}

#repr(c)
Empty::struct {
}
