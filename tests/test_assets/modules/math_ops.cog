module math;

export make_pair::(x: i32, y: i32) -> Pair {
    return Pair { x = x, y = y };
}

export sum_pair::(pair: Pair) -> i32 {
    return pair.x + pair.y;
}

export main::() -> i32 {
    return 99;
}
