Color :: enum(u16) {
    Red,
    Green,
    Blue,
}

cast_source: s32 = 10;

cast_s64_test: s64 = cast(s64, cast_source);
cast_color_test: Color = cast(Color, 0);
cast_enum_raw_test: u16 = cast(u16, Color.Green);