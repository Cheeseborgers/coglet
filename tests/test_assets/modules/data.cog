module data;

export answer :: 40;
export step :: 2;

export counter: s32 = 1;
export values: s32[3] = [2, 3, 4];

export Pair::struct {
    x: s32;
    y: s32;
}

export pair: Pair = Pair {
    x = 5,
    y = 6,
};

export bump::() -> void {
    data.counter += 1;
}
