module data;

export answer :: 40;
export step :: 2;

export counter: i32 = 1;
export values: i32[3] = [2, 3, 4];

export Pair::struct {
    x: i32;
    y: i32;
}

export pair: Pair = Pair {
    x = 5,
    y = 6,
};

export bump::() -> void {
    data.counter += 1;
}
