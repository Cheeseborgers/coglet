TOD0: Array initializer checking

This is the best next feature if you want to keep improving semantic completeness.

Catch:

values: i32[3] = [1, 2];       // too few
values: i32[3] = [1, 2, 3, 4]; // too many
values: i32[3] = [1, true, 3]; // wrong element type

Allow:

values: i32[3] = [1, 2, 3];
values: i32[] = [1, 2, 3]; // later maybe infer size
// values: i32[3]{1,2,3};  // to add!!

But this needs parser/AST support if not already there.


////////////////////////////////////////
Coglet compiler.

Current status:

Parser
✓ expressions
✓ functions
✓ structs
✓ enums
✓ switch
✓ casts
✓ break/continue
✓ compound assignment

Semantic
✓ scopes
✓ type checking
✓ constants
✓ enums
✓ switch exhaustiveness
✓ return analysis
✓ constant array bounds
✓ casts
✓ break/continue
 strings

Language
□ slices  (when we can compile ourselves?)
□ imports
□ modules
□ generics (when we can compile ourselves?)

Codegen
(current status...)

Goals:
- Modern systems language (jai/zig/odin/etc..)
- Clean compiler architecture
- No spaghetti code
- Correctness over cleverness
- Building toward LLVM backend/ not sure on this yet

