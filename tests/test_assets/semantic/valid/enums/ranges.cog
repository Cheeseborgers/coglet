EnumU8Max :: enum(u8) {
    A = 254,
    B,
}

EnumI8Range :: enum(i8) {
    A = -128,
    B,
    C = 127,
}

EnumDefaultLargeEnough :: enum {
    A = 255,
    B,
}

SmallEnum :: enum(u8) {
    A,
    B,
}

GOOD_CAST_ENUM_U8_MAX :: cast(EnumU8Max, 255);
