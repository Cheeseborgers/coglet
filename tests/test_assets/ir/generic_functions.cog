identity::<T>(value: T) -> T {
    return value;
}

main::() -> s32 {
    left: s32 = identity(40);
    right: s32 = identity(2);
    wide: s64 = identity::<s64>(7);

    if wide != 7
        return 1;
    return left + right;
}
