Mode::enum(s8) {
    Negative = -1,
    Active = 3,
}

check_mode::(mode: Mode) -> s32 {
    if mode != Mode.Active
        return 1;
    if cast(s16, mode) != 3
        return 2;
    return 0;
}

main::() -> s32 {
    return check_mode(Mode.Active);
}
