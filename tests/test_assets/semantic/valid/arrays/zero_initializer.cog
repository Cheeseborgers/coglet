Holder::struct {
    values: i32[3];
}

takes_values::(values: i32[3]) -> i32 {
    return values[0] + values[1] + values[2];
}

make_values::() -> i32[3] {
    return {0};
}

exercise::() -> i32 {
    values: i32[3] = {0};
    values[1] = 7;
    values = {0};

    holder := Holder {
        values = {0},
    };

    made := make_values();
    return takes_values({0}) + holder.values[2] + made[0];
}
