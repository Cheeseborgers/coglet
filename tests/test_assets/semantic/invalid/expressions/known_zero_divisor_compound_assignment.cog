ZERO :: 0;

test_known_zero_divisor_compound_assignment::(
    integer: s32
) -> void {
    integer /= 0;
    integer %= 0;

    integer /= ZERO;
    integer %= ZERO;

    integer /= cast(s32, 0);
    integer %= cast(s32, 0);

    integer /= (1 - 1);
    integer %= (1 - 1);
}
