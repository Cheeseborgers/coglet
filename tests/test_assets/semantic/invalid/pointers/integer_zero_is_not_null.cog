takes_pointer::(pointer: s32*) -> void {
}

returns_pointer::() -> s32* {
    return 0;
}

ZERO_POINTER: s32* : 0;

test::() -> void {
    pointer: s32* = null;
    pointer = 0;

    invalid_initializer: s32* = 0;

    takes_pointer(0);
}
