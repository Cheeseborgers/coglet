min::<T: ordered>(a: T, b: T) -> T {
    if a < b
        return a;
    return b;
}

select::<T, U: integer>(left: T, right: U) -> T {
    return left;
}

main::() -> s32 {
    a := min(10, 20);
    b := min::<s64>(30, 40);
    c := select::<s32, u64>(a, 7);
    return c;
}
