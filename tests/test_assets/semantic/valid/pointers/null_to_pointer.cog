NULL_S32 :: cast(s32*, null);

identity::(pointer: s32*) -> s32* {
    return pointer;
}

test_null_pointer_cast::() -> s32* {
    pointer := cast(s32*, null);

    pointer = NULL_S32;
    pointer = identity(cast(s32*, null));

    return cast(s32*, null);
}
