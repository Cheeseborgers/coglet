identity::<T>(value: T) -> T {
    return value;
}

main::() -> i32 {
    left: i32 = identity(40);
    right: i32 = identity(2);
    wide: i64 = identity::<i64>(7);

    if wide != 7
        return 1;
    return left + right;
}
