ONE :: 1;

test_known_nonzero_divisor::(
    integer: s32
) -> void {
    integer / 1;
    integer % 1;

    integer / ONE;
    integer % ONE;

    integer / cast(s32, 1);
    integer % cast(s32, 1);

    integer / (2 - 1);
    integer % (2 - 1);

    integer /= 1;
    integer %= 1;

    integer /= ONE;
    integer %= ONE;

    integer /= cast(s32, 1);
    integer %= cast(s32, 1);

    integer /= (2 - 1);
    integer %= (2 - 1);
}
