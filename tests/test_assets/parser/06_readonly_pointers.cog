Point::struct {
    x: s32;
    y: s32;
}

inspect::(
    value: readonly s32*,
    point: readonly Point*,
    nested: readonly u8**
) -> readonly s32* {
    local: readonly s32* = value;
    return local;
}
