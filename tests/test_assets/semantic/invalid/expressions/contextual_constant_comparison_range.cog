equal_too_large::(x: u8) -> bool {
    return x == 256;
}

ordered_too_large::(x: u8) -> bool {
    return x < 256;
}

equal_negative_unsigned::(x: u8) -> bool {
    return x == -1;
}

ordered_negative_unsigned::(x: u8) -> bool {
    return -1 < x;
}

equal_s8_too_large::(x: s8) -> bool {
    return x == 128;
}

equal_s8_too_small::(x: s8) -> bool {
    return x == -129;
}
