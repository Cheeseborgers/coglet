Point::struct {
    x: s32;
}

mutate_readonly_storage::(
    value: readonly s32*,
    values: readonly s32*,
    point: readonly Point*
) -> void {
    *value = 1;
    values[0] = 2;
    (*point).x = 3;

    *value += 1;
    (*value)++;
}
