opaque_types::(p: s32*, h: opaque*, rh: readonly opaque*) -> void {
    slot: opaque* = null;
    out: opaque** = &slot;
    erased: opaque* = reinterpret(opaque*, p);
    recovered: s32* = reinterpret(s32*, h);
    view: readonly opaque* = rh;
}
