#repr(c)
CPoint::struct {
    x: c_int;
    y: c_double;
    next: readonly CPoint*;
}

#extern(c)
consume_point::(point: CPoint) -> c_int;

#repr(c)
CMode::enum(c_int) {
    Idle = -1,
    Running = 3,
}

#repr(c)
CHandle::struct;
