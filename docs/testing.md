# Testing

The .Expected files for invalid semantic tests can be generated by running get_expected.sh  

## Checking a single files semantics:

```bash
❯ ./cmake-build-debug/check_semantics \
    tests/test_assets/semantic/invalid/types/shadowed_struct_is_distinct.cog

echo $?
```
