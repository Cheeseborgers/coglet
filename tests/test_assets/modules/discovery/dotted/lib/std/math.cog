module std.math;

export answer :: 39;

export Mode::enum(u8) {
    Zero,
    One,
}

export Pair::struct {
    x: s32;
    y: s32;
}

export shared: Pair = Pair { x = 18, y = 20 };

export add::(a: s32, b: s32) -> s32 {
    return a + b;
}
