Point :: struct {
    x: i32;
}

mutation_targets_are_valid :: () {
    point := Point {
        x = 0,
    };

    values: i32[3] = [0, 0, 0];

    point.x = 1;
    point.x += 1;
    point.x++;

    values[0] = 1;
    values[1] += 1;
    values[2]++;
}