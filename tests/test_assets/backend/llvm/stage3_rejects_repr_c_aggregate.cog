#repr(c)
CPoint::struct {
    x: c_int;
    y: c_int;
}

identity::(point: CPoint) -> CPoint {
    return point;
}

main::() -> i32 {
    point := CPoint {
        x = 1,
        y = 2,
    };
    result := identity(point);
    return cast(i32, result.x + result.y);
}
