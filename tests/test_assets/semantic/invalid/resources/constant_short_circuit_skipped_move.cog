Handle::resource { value: s32; }
consume_bool::(value: Handle) -> bool { return true; }

test::() -> void {
    value := Handle { value = 1 };
    ignored := false && consume_bool(move value);
    value = Handle { value = 2 };
}
