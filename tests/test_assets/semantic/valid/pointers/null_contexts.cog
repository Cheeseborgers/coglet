identity::(pointer: s32*) -> s32* {
    return pointer;
}

test_null_contexts::() -> s32* {
    value: s32 = 10;

    // Typed variable initialisation.
    pointer: s32* = null;

    // Assignment to an existing pointer.
    pointer = null;

    // Function argument with a known parameter type.
    pointer = identity(null);

    // Return expression with a known return type.
    return null;
}
