module std.math;

export answer :: 39;

export Mode::enum(u8) {
    Zero,
    One,
}

export Pair::struct {
    x: i32;
    y: i32;
}

export shared: Pair = Pair { x = 18, y = 20 };

export add::(a: i32, b: i32) -> i32 {
    return a + b;
}
